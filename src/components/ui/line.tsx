export default function UILine({ children }: { children?: React.ReactNode }) {
  return (
    <div className="ml-1.5 flex h-15 items-center gap-5 border-l-2 border-primary pl-10">
      {children}
      aaaaaaaaa
    </div>
  );
}
