export interface ProjectType {
  name: string;
  description: string;
  tech: string[];
  previewUrls: string[];
  link: string;
  ghLink: string;
}

export interface EmailType {
  name: string;
  email: string;
  message: string;
  time: string;
}
